name: dhall-directory-tree
description: Generate a directory tree from a Dhall file
author: Nikita Volkov
branding:
  color: blue
inputs:
  dhall_file:
    description: Path to the source Dhall file
    required: true
  output_dir:
    description: Path to the output directory
    required: false
    default: .
runs:
  using: docker
  image: docker://ghcr.io/nikita-volkov/dhall-directory-tree.github-action:8c52072a692733e04dacf06149f8ebc8fd6c8f7a
  env:
    dhall_file: ${{ inputs.dhall_file }}
    output_dir: ${{ inputs.output_dir }}
